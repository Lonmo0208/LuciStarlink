# 监工方评审与下一轮任务（写给实施窗口）

更新时间：2026-09-18 晚（第二次修订，已按 `1137c15` 与 ARCH-V2 §5 的实测数据修正我自己此前两处错误指令）。
审查依据：`git log` 到 `1137c15` + 当前工作树 + 你重写后的 `docs/ARCH-V2-GLOBAL-STORAGE.md`。

**一句话**：你把我给的方案逐个用实验证伪（piggyback、prompt posting、同包 mixin、以及"原地覆盖能修好偏差"），并纠正了测量口径与统计方法的污染——这些我都接受。
现在**真正挡在前面的是两件事**：block 偏差根因未定位（正确性）与 `sky_hole` 仍落后 ~10%（性能）。

---

## 1. 我接受/确认的结论（此后不再讨论）

1. **验收口径**：per-pass 最小值（jsonl 的 `minPassNanos`）的**中位数**，≥5 次重复，所有引擎一视同仁；"wall ≤ 3.0 ms"作废。约定线：**≤1.0 ms**。
2. **`directSectionInstall` 保持关闭**：它不比默认快（0.599 vs 0.651，噪声内），且**与原地/换对象实现无关的分歧率仍在**，并且它跳过引擎复查、没有兜底。
3. **原地覆盖（不替换 `DataLayer` 对象）是"由线程模型推理得出的硬要求"** —— 但不能说它修好了偏差：
   换对象版 5 跑 3 偏、原地版 5 跑 2 偏，**偏差根因仍未定位**（这是你的结论，我此前写的"3 次 1 偏 vs 0"已作废，不要再引用）。
4. **偏差统计的污染已确认**：每次运行都会重存世界，而 `haloPublish=false` / `adoption=false` 这类诊断跑会把世界改成另一种光照状态 → 参考值本身在漂移；**此前"7 对 / 6 偏"的比例不能当偏差率用**。
5. **探针的有效域**：只在**无流体气层**上判定；全列 block 指纹**连 vanilla 都不可复现** → 整列不可作判据（新增到勿重测清单）。
6. **piggyback 已否决**（per-pass min 0.48 → 1.24 ms + 14.6 ms 卡顿）；**prompt posting 也已否决**（0.776 → 1.114 ms，+44%）：
   **250 µs 合并窗口是收益**（把一波发布合并成更少的 drain 轮次），"更早投递"不是杠杆，**"更少的轮次 / 更少的工作"才是**。
7. **同包 mixin 不可行**（NeoForge 模块路径 `ResolutionException`，服务器起不来）。
8. sorter-mailbox 回调本来就在光照线程 → 多一条任务是队列跳，不是线程唤醒。

## 2. 当前战报（同 rig，per-pass min 口径，中位数）

| 引擎 | `sky_hole` | 其余三档（wall，同日） |
|---|---|---|
| vanilla | ~1.00 ms | — |
| ScalableLux | **0.593 ms** | 18.8–26.3 / 12.0–16.2 / 19.9–28.5 |
| **我们（默认）** | **0.651 ms**（落后 ~10%） | **9.49 / 5.25 / 8.40**（大幅领先） |

## 3. 下一轮：两件并行的第一优先

### P1（正确性，最高优先）：定位 block 偏差的根因
它是**唯一挡在 storage 模式前面的东西**，也决定"我们的光照与 vanilla 一致"这句结论能不能成立。
要求：
- **只用相邻成对比较**（同一存档、同一探针、连续两次），且只取**无流体气层窄带**；不要再用跨配置跑出来的比例；
- 明确区分三种可能：(a) 我们写的值与 vanilla 不同（我们错）；(b) vanilla 自身在该带不稳定（不是我们错）；(c) 探针/世界重存引入的漂移（测量问题）。
  **判据**：把 (b) 与 (c) 各自排除掉或证明掉，若剩下 (a)，再定位到具体 section/层/单元格并以"逐格差异清单"形式给出；
- 结论以"vanilla 对照 + 相邻成对 + 窄带"三条件齐备的表格呈现。

### P2（性能）：按"更少的轮次 / 更少的工作"推进 `sky_hole`
按你自己写进 ARCH-V2 阶段 2 的结论，依次尝试（每步 per-pass min 中位 ≥5 次）：
- **A1 加长合并窗口**（`publishCoalesceNanos` 可配：250 µs / 500 µs / 1 ms）；
- **A2 减少每轮发布的 section 数**（同 chunk 多 section 合并成一次提交；复核 `skippedIdentical` 空间）；
- **A3 批次入口统一**（每 tick 一波、整批一次 drain，把轮次从"每作业"降到"每 tick"）；
- 三条都不够时，再评估 **B：服务端线程内联"算 + 装"**（必须过"相邻成对探针 0 偏差"门槛）。
判据：per-pass min 中位 ≤ 0.593 ms（追平 SL），或显著下降且 wall / 其余三档不回归。

### P3（证据）：补完四档 per-pass min 对照表（≥5 次）
目前只有 `sky_hole` 有可信数据；这张表齐了才能谈"四档全胜"与任何默认值。

## 4. 任何改动都必须过的门槛（缺一不可）

1. 差分套件全绿（21 项 / 0 失败 / 1 继承跳过）；
2. **相邻成对偏差探针 ≥3 对、0 偏差**（无流体气层窄带），且与 vanilla 逐位对照；
3. per-pass min 中位（≥5 次）：四档不低于 ScalableLux；
4. 存档往返：放灯 → 同 tick `save-all flush` → 重启 → 无报错 + 指纹一致；
5. 客户端同步：dev 客户端连 dev 服务端、边界放光源屏幕立刻亮（当前以 `publish.notify.posted` 计数 + 存档往返指纹作间接证据，最终要真人用例）；
6. **不许拿"关掉正确性"换数字**（`haloPublish=false` / `runtimeHaloChunks=0` / 跳过复查一律作废）。

## 5. 我（监工）会独立复验什么

- 改动范围：`git log` / `git status` / `git show`（现在有版本控制了，不再靠 md5 猜）；
- 构建与测试：`mc-smoketest/verify.sh`（自动 JDK 21 + TLS + 21 测试 + rig 元数据 + 四路对照）；
- 存档用例：`mc-smoketest/verify-extra.sh` 用例 A；
- 客户端用例：按 `verify-extra.sh` 用例 B 清单执行（需要真人或给 client run 加 `--quickPlayMultiplayer`）；
- 数字：我**自己读 jsonl 的 `minPassNanos` 算中位数**，不看你的结论。

## 6. 勿重测清单（已排除）

halo 机制、测量偏差、"我们的天空算法慢"、每作业流水线交接、通知扇出、合并延迟**单独**（现已证明合并是收益）、`runtimeHaloChunks=0`、高度图天空列、
**piggyback**、**prompt posting**、**同包 mixin**、worldgen 只发核心区作为默认（~4%，保留为可选项）、**整列 block 指纹作为判据**（vanilla 自身不可复现）。

**不要做**：在缺"相邻成对探针 0 偏差"证据的情况下翻任何默认值（含 `directSectionInstall`、`worldgenHaloPublish`、`lightEngineMode`）。
