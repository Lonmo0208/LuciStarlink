# 任务：让 `sky_hole` 追平或超过 ScalableLux（外加 dense 的偶发卡顿）

状态：**进行中，尚未达成**。接手人：监工（本轮起全权负责实现与验证）。日期：2026-09-18。

## 1. 目标与验收（先把判据写死，避免又出现"两张表对不上"）

**主目标**：`sky_hole` 的 per-pass min（≥5 轮中位数）**不高于同场交错的 ScalableLux**，也就是比值 ≤ 1.00。
**次目标**：`dense_chunk_patch` 的 wall 尾巴（偶发 ~30 ms 的 pass）定性并压掉，wall 比值从 2.5–3.1× 降到 ≤1.5×。

**验收方式（沿用已生效的口径，不再另立）**：
1. 同会话**交错** A/B（我们的两次配置逐轮交替，或我们↔SL 逐轮交替），各 ≥5 轮，机器上无其它负载；
2. 统计量 = `minPassNanos` 中位数；**必须附精确双尾 Mann-Whitney U/p**（`mc-smoketest/ab-compare.ps1`）；
3. **独立复现一次**：比值可复现才算数（单组结果只作线索）；
4. 正确性门槛：差值套件 21 项全绿 + 相邻成对偏差探针 0 偏差 + `docs/HANDOVER.md` 的客户端用例；
5. 其它三档不得回归（`block_toggle_border` / `structure_cube` / `dense` 的 per-pass min）；
6. 任何改动都要**一行可回退**（开关或默认值），并写进 CHANGELOG / NOTICE。

## 2. 现状（本轮开始时实测；**已被 §7.8–7.9 更正：洁净协议下是 ~2.0 倍，不是 25–35%**）

| 项 | 我们 | SL | 比值 | p |
|---|---|---|---|---|
| `sky_hole` per-pass min（ab 组） | 0.767 ms | 0.572 ms | 1.341 | 0.056 |
| `sky_hole` per-pass min（ab2 复制组） | 0.666 ms | 0.535 ms | 1.246 | 0.056 |
| `sky_hole` wall | 4.87 / 4.26 | 4.71 / 2.67 | 1.03 / 1.59 | 0.55 / 0.008 |

两组比值稳定（1.34 → 1.25），方向与量级复现 → 这是真差距，`sky_hole` 上我们**稳定慢 25–35%**。

## 3. 一次 pass 的成本预算（第一版，已被 §7 部分推翻，保留作为过程记录）

一次 pass = `applyPattern`（25 次 setBlock）+ 等光照流水线排空。实测拆分（`ab-ab2-ours-sky_hole-r3.log`，最好那一轮）：

| 组成 | 耗时 | 说明 | SL 是否也要付 |
|---|---|---|---|
| 应用 25 次改动（`bench.apply_pattern` 0.496 ms / 4 pass） | ~0.12 ms | 纯 setBlock + 引擎侧记账 | **要付**（共同成本） |
| 合并窗口延迟（`publish_batch.queueLatency`） | ~0.25 ms | 250 µs 定时器 + `delayedExecutor` 的定时线程跳 | 不付 |
| drain 里的发布（`publish_direct`） | ~0.10 ms | 打包 nibble + 分配 `DataLayer` + 与引擎现状比对 | 不付 |
| drain 里的引擎自更新（`publish_batch.runLightUpdates`） | ~0.15 ms | 我们把 section 交回引擎后，引擎自己的 `runLightUpdates()` | 不付（SL 就地写） |
| drain 里的客户端通知（`publish_batch.notify` + `publish.notify.posted`） | ~0.15 ms | 每 section 向 27 邻位排队、去重后平均 6.6 次 `onLightUpdate` | 部分 |
| **合计** | **0.77 ms** ✓ 与 `minPassNanos` 吻合 | | |

`sky_hole` 一次运行（100 次改动）= 发布 **1541 个 section**（其中 **764 是 halo**）+ **10208 次通知** + 跳过 984 个"与引擎相同"的 section。
`regionChunks=1` + `runtimeHaloChunks=1` → 映像是 3×3 区块，核心只占 1/9，**其余 8 块都是 halo**。

**当时的结论（现已修正）**：差距 = 我们独有的交付开销。见 §7：L2 实测证明通知侧不是瓶颈。

## 4. 杠杆清单

| # | 杠杆 | 预估收益 | 风险 | 状态 |
|---|---|---|---|---|
| L1 | 合并窗口 250 µs 调小 | ~0.1–0.2 ms | 窗口过小 → 一次 pass 被拆成多次 drain，反而更差（`promptRuntimePublish` 实测 +44%） | **已测：否定**（§7.1） |
| L2 | 通知扇出 27 → 1（学 SL：只通知真正变化的 section） | ~0.05–0.1 ms | 客户端边界渲染可能缺数据 → 必须过客户端用例 | **已测：机制生效、耗时无可测变化**（§7.2） |
| L3 | 发布体积（纯天空改动不打包 block 层；halo 只发真正跨界的 section） | ~0.05–0.15 ms | 边界黑缝回归 → 必须过探针 + 客户端用例 | 优先级下调（§8） |
| L4 | 打包零分配（直接写进引擎已有 `DataLayer`） | ~0.02–0.05 ms | 低（原地覆盖本就是硬要求） | 优先级下调 |
| L5 | `directSectionInstall`（跳过引擎回吸收） | ~0.1 ms（−13%，未显著） | **正确性**：跳过引擎复查，世界生成期会与 vanilla 块光分歧 → 现为默认关 | §8 提出"只对运行期路径开"的变体 |
| L6 | 砍 halo（`runtimeHaloChunks=0`） | 大 | 跨区块黑缝直接回归 → **不做** | 否决 |
| **L7** | **按来源给"等待"归因**（采样三个 pending 标志），量出这 0.2 ms 里有多少是"我们自己的活" | 诊断 | 无（只是量具） | **下一步（§8）** |
| **L8** | **运行期发布优先于世界生成发布**（两者共用同一条光照线程发布队列，小编辑排在大批量后面） | 未估 | 排序改动不动光值只动延迟；需回归三档 | 待 L7 归因后决定 |

## 5. 第二个目标：`dense_chunk_patch` 的 wall 尾巴

两组实测 wall 都是我们慢 2.5–3.1×（34.7/31.6 vs 11.1/12.7，p=0.151/0.095，方向一致），已知"四分之一到一半的 pass 偶发 ~30 ms"。
下一步先做**归因**（不是优化）：用每轮增量埋点（§9）确认那 30 ms 落在哪一段；归因清楚前不动手。

## 6. 禁止事项（写下来防复发）

- 不得用顺序组（非交错）的数字签发结论；
- 不得只看中位数不报 U/p；
- 不得为了 `sky_hole` 的数字牺牲跨区块正确性（halo / 回吸收 / 存档安全三样）；
- 不得在没有探针证据的情况下打开任何"跳过引擎复查"的开关。

## 7. 本轮实测结果（交错 + 精确 p）

### 7.1 L1 合并窗口 250 µs → 50 µs：**否定**
`sky_hole`，5 轮交错：中位 1.116 → 0.970（比值 1.150），U=15/25，**p=0.73**。方向看着好 13%，但在噪声内；**远不足以**吃掉 25–35% 的差距。

### 7.2 L2 通知扇出 27 → 1：**机制完全生效，耗时无可测变化**
`sky_hole`，10+10 轮交错：

| 变体 | 10 轮 minPass（ms） | 中位 | 通知次数 | 发布 section | drain |
|---|---|---|---|---|---|
| ON（现状，27 邻位） | 0.880 0.561 0.778 1.274 1.272 0.675 0.695 1.044 0.500 0.510 | 0.737 | **10015** | 1468 | 45 |
| OFF（只通知本体） | 0.589 1.291 0.816 1.214 0.516 0.871 0.765 1.069 0.440 1.169 | 0.844 | **1401** | 1437 | 47 |

**p=0.68**。机制量证明改动生效（通知 7.1×↓、发布量与 drain 次数不变），所以结论是：**通知扇出不是瓶颈**。这推翻了 §3 里"notify ≈ 0.15 ms"的预算。

### 7.3 一个重要事实：**apply 侧我们比 SL 快**

| 组 | n | minPass | applyTot | waitTot | 每次改动 apply | 每次改动 wait |
|---|---|---|---|---|---|---|
| 我们（扇出 ON） | 10 | 0.737 | **0.37** | 4.03 | **4 µs** | 40 µs |
| 我们（扇出 OFF） | 10 | 0.844 | **0.41** | 3.78 | **4 µs** | 38 µs |
| ScalableLux | 5 | 0.572 | 0.47 | 3.42 | 5 µs | 34 µs |
| ScalableLux（复制组） | 5 | 0.535 | 0.48 | 1.39 | 5 µs | 14 µs |

SL 的 apply 比我们大（同步完成，光值当场算完）；我们的 apply 更小（活被推到异步流水线）。**差别在 wait**：我们的 wait 里混着"与本次编辑无关"的在飞工作——慢轮里 `drains=0 tasks=0 sections=0`（本轮什么都没发布）却等了 1.2 ms，那是基准的**全局排空栅栏**在等别的批次（世界生成/预热遗留）；而 SL 的同步模型根本没有"待处理队列"可等。

所以正确表述不是"我们的引擎慢 25–35%"，而是：**我们的编辑成本被记在一条共享排空栅栏上，而 SL 的编辑成本是同步的、天然只含它自己的活。**

### 7.4 分段埋点定位到**队头阻塞**：`queueLatency` 平均 0.91 ms

同一阶段（4 个测量 pass 所在的光照阶段）的分段量：

| 指标 | 总量 / 次数 | 每次 |
|---|---|---|
| `publish_batch.queueLatency`（任务入队 → drain 开始） | 23.781 ms / 26 | **0.91 ms** ← pass 路径上最大单项 |
| `publish_batch.drain`（drain 本体） | 9.717 / 26 | 0.37 ms |
| `publish_batch.runLightUpdates` | 3.502 / 26 | 0.13 ms |
| `publish_batch.notify` / `notify.post` | 3.129 / 3.069 | 0.12 ms |
| `publish_direct`（打包+交付） | 2.656 / 46 | 0.058 ms |
| `stage.runtime.job.runtime`（我们的计算） | 2.407 / 19 | 0.13 ms |
| `stage.runtime.incremental.*`（天空+块+材料+发布） | ~0.97 / 19 | 0.05 ms |
| `enqueue_block_change` | 0.178 / 104 | 1.7 µs |

同时段**世界生成**在同一批 worker 上跑了 34 个区块：`light_chunk.compute` 106 ms、`worker_compute` 77 ms、`stage.worldgen.extract` 28 ms、`stage.worldgen.block` 24 ms。

**结论（本轮最重要的定位）**：运行时编辑的成本**不在计算**（0.13 ms）**也不在通知**（L2 已证伪），而在**等 drain 开跑**（0.91 ms）。drain 之所以起不来，是因为光照线程正忙于**世界生成的批量发布**（两者共用同一条光照线程队列）。这也解释了 L1 为什么无效：250 µs 窗口只是这个等待的一小部分，真正的时间花在"上一轮 drain 还没结束"。

### 7.5 L8 发布分道（运行期优先 + 世界生成批量限流）：**否定**
`sky_hole`，10+10 轮交错：中位 0.828 → 0.638（比值 1.298），但 **p=0.63**（两组各有两个 ~1.1 ms 的离群轮，中位被离群轮落点决定）。
更关键的是**机制量没有动**：目标指标 `publish_batch.queueLatency` 平均 OFF 中位 0.885 ms vs ON 中位 0.997 ms —— 分道没有减少排队等待本身。
→ 改动保留为开关（`-Dlucistarlink.publishPrioritiseRuntime=`，**默认 false**），但不翻默认。

### 7.6 第四个假设（基准每轮重建世界导致后台世界生成污染）也被否定
保留世界（`KEEP_WORLD=1`，10 轮，同配置）：0.814 1.009 1.516 1.038 1.082 0.966 0.650 1.521 1.075 0.968 → 中位 **1.009**、极差 1.34×，比重建世界的组**更慢也更散**。所以后台世界生成不是散布的来源。

### 7.7 目前最锐利的特征：**我们有"偶发 2 倍慢"的尾巴，SL 没有**
汇总本轮所有采样（同配置、交错、无并发负载）：

| 来源 | 采样值（ms） | 中位 | 最大 |
|---|---|---|---|
| 我们（多个组） | 0.44 ~ 1.52 | 0.64 ~ 1.01 | **1.27 ~ 1.52**（约 1/4 的轮次落在 1.1 以上） |
| ScalableLux（ab / ab2） | 0.404 ~ 0.677 | 0.535 / 0.572 | **0.677** |

所以准确的表述不是"我们平均慢 25–35%"，而是：**我们有一条约 25% 概率被触发的"2 倍慢"路径，SL 没有。** 四次归因尝试（合并窗口、通知扇出、发布分道、保留世界）都没能消掉它。

## 7.8 协议缺陷被找到并修好：慢轮的成因是**世界生成的光照活落进测量窗口**

每轮归因显示：慢轮的全部四个 pass 都是 1.1~1.6 ms 且 `drains=0 tasks=0 sections=0`（本轮什么都没发布），快轮总有一个 ~500~800 µs 的 pass；而世界生成**总量**在快慢轮里相同（101 ms 都有）→ 不是量的差别，是**时序**。

机制：测量用的是引擎的"该区块待办完成"future，而**生成一个紧邻测量区块的新区块，会给测量区块本身排队光照活**（光要跨界）。基准的预生成范围只有 +1 区块、静默又是"一次快照"，所以邻区块的生成与光照会在测量轮里才落地。

修正（两个可调项）：`-Dlucistarlink.benchmark.prepareRing=3`（预生成外扩 3 区块，prepareChunks 64 → 144）+ `-Dlucistarlink.benchmark.quiesceSettleMs=1000`（静默需连续 1 秒无待办）。**同一引擎 A/B（6+6 交错）**：

| 组 | 6 轮原始值（ms） | 中位 | p |
|---|---|---|---|
| ring=1（旧协议） | 0.803 0.823 0.827 0.812 0.963 1.164 | 0.825 | — |
| ring=3 + 稳定窗 1000 ms | 0.550 0.575 0.796 0.659 0.490 0.385 | **0.562** | **U=36/36，p=0.0022** |

## 7.9 洁净协议下重测我们↔SL（10+10 交错，p<0.0001）——**结论比原来更差，必须更正**

| 组 | 10 轮原始值（ms） | 中位 | 范围 |
|---|---|---|---|
| **ScalableLux** | 0.323 0.407 0.351 0.347 0.422 0.366 0.358 0.349 0.353 0.432 | **0.355** | 0.32–0.43 |
| 我们 | 0.753 0.727 0.514 0.605 0.754 0.690 0.822 0.906 0.499 0.544 | **0.708** | 0.50–0.91 |

**U=100/100（完美分离），p<0.0001 → `sky_hole` 上我们是 SL 的 2.0 倍耗时。**

两个协议下 SL 的数字：旧 0.535/0.572 → 洁净 **0.355**；我们：旧 0.666/0.767 → 洁净 0.708。也就是说**旧协议把两边的数字都推高了、而且把 SL 推得更多**，于是"落后 25–35%"是个被压缩过的假象。

**必须同步更正**：`README.md` / `CHANGELOG.md` / `NOTICE` 里"落后 25–35%（Fisher≈0.02）"的表述改为"洁净协议下落后约 2.0 倍（0.708 vs 0.355，p<0.0001）"，并注明旧数字的协议缺陷。这是本轮最重要的更正。

**我们的成本结构（洁净协议下与预算吻合）**：apply ~0.12 + 合并窗口 ~0.25 + drain（发布+引擎回吸收+通知）~0.4 ≈ 0.77 ms；SL 的 0.355 里 apply（含**同步**光照）约 0.3。**差距 = 我们不交付、不排队的部分**（合并窗口 + drain + 回吸收），与 §3 的预算一致，只是现在才是干净的测量。

## 8. 下一步（本轮结束时的判断，已按 §7.4–7.9 收敛）

**主结论：第六个假设（协议缺陷）成立并已修好；在此之前的五个假设全部被实测否定。目标未达成，而且洁净协议下差距比原文档写的更大（~2.0 倍）。**

1. **协议优先**：所有后续测量必须带 `-Dlucistarlink.benchmark.prepareRing=3 -Dlucistarlink.benchmark.quiesceSettleMs=1000`，并且**四档都要用洁净协议重测一遍**（现存的 block_toggle / structure / dense 数字都是旧协议的，SL 侧受影响更大，结论方向虽不至于翻转，但幅度不可信）。
2. **差距的构成已经清楚**（§7.9）：合并窗口 ~0.25 + drain（发布 + 引擎回吸收 + 通知）~0.4，都是"我们不交付、不排队就不用付"的部分。所以接下来的杠杆只能是**减少交付本身**：L5（`directSectionInstall`，跳过引擎回吸收）——正在洁净协议下重测；以及 L3（发布体积）。
3. L5 有一个必须正面解决的正确性问题：它跳过引擎对其提交 section 的复查，世界生成期会与 vanilla 块光分歧。若洁净协议下它确实把差距吃掉，方案是**只对运行期路径开**（运行期已有外部刷新 + 漂移重跑兜底），并补探针证据。
4. `dense_chunk_patch` 的 30 ms 尾巴也要用洁净协议重看——它很可能同样是旧协议污染。
5. 已证伪、不要再试的（本轮）：合并窗口（L1）、通知扇出（L2）、发布分道（L8）、保留世界、400 ms 稳定窗单独使用。

## 10. 洁净协议下的杠杆重测（第二轮，目标：四档持平）

### 10.1 `directSectionInstall`（跳过引擎回吸收）：**否定** —— 而且它动摇了 V2 的前提
洁净协议、n=10+10（sky_hole 与 dense 两档）：

| 负载 | OFF | ON | 比值 | p |
|---|---|---|---|---|
| `sky_hole` | 0.769 | 0.745 | 1.031 | **0.63** |
| `dense_chunk_patch` | 1.879 | 1.839 | 1.022 | **1.00** |
| `sky_hole` wall | 4.216 | 4.043 | 1.043 | 0.74 |
| `dense` wall | 8.877 | 9.155 | 0.970 | 0.80 |

原始值两组的分布几乎完全重合（各含一个 ~1.2 的离群轮）。**n=6 时看到的 −37% 是噪声。**

**三个结论**：
1. "把 section 交回引擎、让引擎复查一遍"这一步的代价只有 ~3%，**不是**我预算里的 0.15 ms；
2. 因此**"差距在交付层"这个前提没有被证实** → V2（存储模式）的预期收益需要重新评估，**不能凭预算开工**；
3. 也说明我 §3/§7.9 的成本预算是"按推理拆的"，不是按实测拆的 —— 下面的"时间线量具"就是为此。

### 10.2 两条线索的更正
- **"我们自己的作业耗时 0.3~11 ms 波动"不成立**：`rtJobUs=5402/12` 是"区间内 12 个作业共 5.4 ms"（每个 ~450 µs），而那个区间比 pass 长（我的埋点窗口跨 tick，`elapsed` 只取引擎 future 的时间戳）。单次作业耗时稳定在 **~130 µs**。线索撤回。
- **"发布分道"（L8）与"内联阈值"否定**的理由现在能统一解释：内联只影响*计算*在哪跑，发布仍走光照线程与合并窗口，所以提高内联阈值单独不会有收益（该项测过 p=0.48）。

### 10.3 剩下的待测项（按可疑度排序）
1. **合并窗口**：洁净协议下重测 250 µs vs 25 µs（旧协议的那次不算数）——窗口是我预算里最大的单项（~0.25 ms）。
2. **`piggybackPublish`**：把发布挂到引擎自己的任务列表（少一次 mailbox 唤醒与合并延迟）；旧协议下测为"更差"，洁净协议下必须重测。
3. **时间线量具**（若上面两项都无效）：在 `LuxBenchmarkSupport` 里放一个环形缓冲，记录 pass 内关键事件的时间戳（apply 结束 / 作业开始结束 / 发布入队 / drain 起止 / future 完成），逐轮打印相对时间 —— 只有它能终结"按推理拆预算"。
4. 若以上全部无效：接受 `sky_hole` 的结构性差距，把 V2 当作**唯一**路线，但要先写清 V2 的收益假设与验收线（回到 §10.1 的教训：先证据、后动刀）。

### 9.0 本轮最后两个实验（洁净协议）
- **合并窗口 250 µs vs 5 ms（n=8+8）**：中位 0.822 → 0.739（p=0.28 不显著），但 **5 ms 组把 1.25–1.40 的慢轮全消掉了**（B 组最大 0.833）。**判定为不可用**：窗口拉长后，pass 的计时在"我们的发布真正落地之前"就结束了（量具少报），所以那不是真提升。顺带确认了 pass 的计时与发布之间存在**错位**：`elapsed` 取自引擎 future 的时间戳，而发布（drain/tasks/sections）可能落在测量窗口之外 —— 这也是为什么很多 pass 显示 `drains=0 tasks=0 sections=0`。
- **内联阈值 8 vs 64（n=6+6）**：中位 0.793 → 0.916（p=0.48）→ 否定。机制量显示该负载本来就大多走内联（`inlineExecuted` 50/54），所以内联/worker 分裂不是双峰来源。
- **新线索（下一步的第一优先）**：同一负载下**我们自己的运行时作业耗时在 0.3 ms 到 11 ms 之间波动**（`stage.runtime.job.runtime` 在单轮窗口内出现过 `5402/12`、`11409/9`、`5304/10` 这样的值）。这才是"2× 慢"的另一半 —— 不在测量侧，而在我们自己的运行期流水线（很可能是同一次编辑被拆成跨多个 tick 的多批处理）。**下一步先量它**：一次 pass 的 25 个改动在引擎里最终被拆成几批、每批多大、跨几个 tick。

## 11. 本轮留下的开关与量具（原 §9）

- `-Dlucistarlink.benchmark.prepareRing=3` 与 `-Dlucistarlink.benchmark.quiesceSettleMs=1000`：**协议修正，后续所有测量都要带**（§7.8）。
- `-Dlucistarlink.notifyNeighbourSections=false/true`（L2 开关，默认 true）。
- `-Dlucistarlink.publishPrioritiseRuntime=true/false`（L8 开关，默认 false）。
- `-Dlucistarlink.publishCoalesceNanos=`（默认 250000）、`-Dlucistarlink.runtime.inlineBatchChanges=`（默认 8）。
- 基准每轮打印 `elapsedUs/applyUs/waitUs/pend` + `drainUs/queueLatUs/notifyUs/runUpdUs/publishUs/notifyPostUs` + `drains/tasks/sections/notifies/identical/qDuringDrain/wgChunks/wgComputeUs/rtTickUs/rtJobUs` 的**本轮增量**。
- 工具：`mc-smoketest/ab-prop.sh`（任意系统属性的交错 A/B，支持 `EXTRA_JVM_A/B`）、`ab-interleaved.sh`（我们↔SL，支持 `EXTRA_JVM`）、`ab-compare.ps1`（分组 + 中位 + 精确 U/p）、`pass-attribution.ps1`（逐轮归因）。

### 10.4 量具终于是诚实的；promptDispatch 被否定；差距定位到计算侧

**(a) 量具修正**：pass 的结束时间改为 `max(引擎 waitForPendingTasks, 我们的发布标记)`。
标记的实现要点：**必须先强制派发本轮改动、再排标记**（否则改动还在运行期缓冲里、标记会提前完成 —— 第一版实测只比 future 晚 10~20 µs，等于什么都没测到）。
修正后实测：标记与引擎 future **同刻（±30 µs）** → 说明"我们的发布与引擎吸收在同一轮 drain 内完成"，即**之前看到的 55 ms 是偶发（某轮发布晚一个 tick），不是系统性延迟**。所以"我们延迟 50 ms、比 SL 差 150 倍"这个当时的推断**不成立，撤回**。

**(b) `promptDispatch`（入队即派发）：否定**
第一版每改动派发一次（193 次/轮）→ 无效；加合并窗口后降到 9 次/轮 ✓ 机制正确，但 n=10 下**显著更差**（中位 0.715 → 0.820，p=0.043）→ 默认保持关闭。原因：多一次唤醒 + 每轮多跑一次 tickRuntime，而在没有"发布晚一个 tick"的轮次里没有任何收益。

**(c) 差距定位**：诚实口径下我们 0.73~1.32 ms vs SL 0.35~0.43 ms（约 2 倍）。而我们的**运行期计算**约 1.2 ms/pass（halo 全开），SL 的**全部**工作才 0.355 ms/pass。
→ **2 倍差距在计算侧**（区域引擎 + 3×3 光环整列修复 vs Starlight 的柱状天空引擎），**不在交付/调度侧**（该侧 10 个假设已全部被否）。

**(d) 下一步：把光环的计算范围收窄到"光真正能到达的地方"**
- 现状：`runtimeHaloChunks=1` 让每个作业计算 3×3 区块的映像；实测 halo=0 使计算量从 7.47 → 3.01 ms（**减半以上**）。
- 现成机制：`RegionLightData.markColumnSectionsWithinReach(..., 15, ...)`（懒加载已用它限定"光能到达的列"）——把**计算范围**也限定到这些列，正确性无损（光不会走超过 15 格）。
- 验收：诚实量具下 n≥10（我们 vs 我们、我们 vs SL）+ 差分套件 + 相邻成对探针 0 偏差 + 客户端边界用例。
- 预期：计算减半 → pass 从 ~0.75 降到 ~0.45-0.55，接近 SL 的 0.35-0.43（可能仍差 ~20%，再评估 L4 零分配与发布体积）。

### 10.5 计算侧的第一次攻坚：找到最大的单一项，但改动被回退（附证据）

**阶段埋点补齐后**（此前 8 ms 未计时），运行期作业的真实构成（halo=1，57 个作业，共 7.76 ms）：

| 阶段 | 每作业 | 说明 |
|---|---|---|
| `job.materializeReach` | **68 µs** | 最大单项（懒加载"光能到达的列"） |
| `incremental.block` | 25 µs | 块光传播 |
| `incremental.publish` | 11 µs | 打包交付 |
| `job.refreshExternal` | 9 µs | 外部刷新 |
| `incremental.sky` | 7 µs | 天空传播 |
| `jobPrep` | 5 µs | |
| 其余 | <5 µs | materialize/materials/changesNearBorder |

**两次诊断假设都被实测推翻，记录下来防复发**：
1. "整列可达集合有 23k 位、遍历它花掉 68 µs" ✗ —— 实测 `materializeReach.bits` = **450/轮**（区域只有 3×3 区块，±15 格的框最多覆盖 9 列 × 24 section = 216 位/改动），所以位遍历不是瓶颈。
2. 改成"列级标记 + 跳过量"后：机制量显示物化 section 数从 **140 → 4680**（33 倍），说明旧代码的标记坐标系与遍历坐标系**对不上**（少物化了本应物化的 section），同时 `job.materializeReach` 从 3.9 ms 降到 0.064 ms、`job.runtime` 从 7.76 → 4.11 ms。
3. **但诚实口径下变慢了**：n=5 对 SL 的比值从 2.0× 变成 **2.82×**（我们 0.872 / SL 0.309，U=25/25）。合理解释：旧代码少物化 → 传播面对的多是空数据 → 活少但可能是错的；新代码物化正确 → 传播的活变多。

**判定**：21 项差分测试**两种代码都通过**，无法判定谁对；改动属于"改语义 + 证据两可"→ **已回退**（出厂行为不变），只保留埋点与 `materializeReach.bits` 计数。
**下一轮必须用探针定性**：在"相邻成对 0 偏差"的判据下比较两种物化行为（旧代码少物化的那 4540 个 section 是否导致光值分歧）。若旧代码确实是错的，那么"快"是假象，差距的很大一部分会被重新解释。

### 10.6 物化问题结清（探针判定），并给出正确解释

相邻成对探针（同一份世界快照、同一次运行时边界编辑、y=90~112 带）：

| 标签 | 原版（引擎关闭） | 我们 | 判定 |
|---|---|---|---|
| strip | f5404bf310045986 / ed8711b3ee4d2d67 | 同 | ✓ |
| **edit（运行时带）** | 013c3846466ff9d5 / af3a8fcaba55adfd | 同 | ✓ |
| edit_border | c4aed9ab13e5d3b5 / a15e9006cdb8d3a4 | 同 | ✓ |
| strip_plus_beyond | 34b45920467bc186 / 1d983741164c0744 | 同 | ✓ |
| beyond | b85c37ba14d2bd5c / 96531bded4264c70 | 同 | ✓ |

**结论（并纠正 §10.5 的第 2 条推断）**：出厂代码物化的是**必需且最小**的集合（~140 个 section/轮），我的"列一致化"版本多物化 24 倍（4680 个）—— 探针证明那些多出来的 section 对光值没有影响，所以旧代码既正确又更省。**"坐标系对不上导致少物化"这个推断撤回**；回退是正确决定。
→ `materializeReach` 的 ~68 µs/作业是"每个必需 section 约 28 µs 的冷读入"（populateSection + 两个层的 adoptLayer，各读 2 KB），是**必要成本**，要再降只能减少必需集合（缩小半径/光环）—— 那属于正确性边界，需要单独论证。

新增的正确性记录：相邻成对探针累计 **8 次 0 偏差**（含本次 5 个标签）。

### 10.7 用诚实量具重测全部交付侧杠杆（旧量具下的结论作废）

诚实量具只测到"发布真正落地"的时刻，而旧量具（引擎队列安静）**看不到我们的发布** —— 所以 §7 里所有交付侧结论都必须重测。重测结果（洁净协议，n=10+10，附精确 p）：

| 杠杆 | 中位（关 → 开） | p | 判定 |
|---|---|---|---|
| 合并窗口 250 µs → 25 µs | 0.807 → 0.780 | 0.44 | **无效** |
| `piggybackPublish`（少一跳/挂引擎任务表） | 0.795 → 0.820 | 0.39 | **无效** |
| `promptDispatch`（入队即派发） | 0.715 → 0.820 | 0.043 | **显著更差**（保持关闭） |
| `directSectionInstall`（跳过引擎回吸收） | 0.754 → 0.605 | 0.44 | 方向 25%，**未显著**（已加测 n=25） |
| 光环 `runtimeHaloChunks` 1 → 0 | 0.828 → 0.806 | 0.74 | 无效（但计算量 2.5~4.5×↓） |

**两个结构性事实**（本轮最重要的两条）：
1. **我们的计算不在关键路径上**：光环开关把计算量从 17.3 → 6.8 ms（物化 12.6 → 2.8 ms），pass 时间**完全不动** → 计算在 worker 上并行，pass 由固定的流水线延迟决定。
2. **那 ~0.8 ms 的固定延迟不由任何单一交付组件构成**：窗口、跳数（piggyback）、通知扇出、回吸收、计算量逐一测过，各自只有 0~4% 的效果。
→ 剩下的解释只能是"**异步引擎 vs 同步引擎**"的口径差本身：SL 在 `setBlock` 内同步算完（0.31~0.43 ms 是它的**全部**成本），我们的 0.75~0.83 ms 是"申请 → 线程跳 → 引擎吸收 → future 完成"这条链路的**固有延迟**。
→ 这与 §10.4 的结论一致，也解释了为什么 12 个交付侧假设全部失败：**它们都在一条约 0.4 ms 的固有链路上做微调**。

**唯一有方向性收益的是 `directSectionInstall`（−25%，p=0.44）** → 已加测到 n=25 定论；若成立，它的正确性代价（跳过引擎复查、世界生成期分歧）需要用"只对运行期开"的变体 + 探针来消化。

### 10.8 通向"持平"的具体路径（方案已定，待实测）

§10.7 的结论是：我们和 SL 的差距是"**异步链路（~0.4 ms 固定延迟）vs 同步完成**"。所以要持平，只有一条路：**给运行期小编辑提供一条"当场算完"的同步通道**，而它需要的三个零件其实都已存在：

| 零件 | 现状 | 作用 |
|---|---|---|
| `directSectionInstall` | 存在，默认关（唯一有收益的，−25% 待 n=25 定论） | 跳过引擎回吸收 |
| `inlineBatchChanges`（默认 8） | 存在 | 让批次在**调用线程**上结算（不大于阈值时） |
| `promptDispatch`（默认关，单独用 −13%） | 已实现（带合并窗口） | 让改动不用等下一个 tick 才被派发 |

**组合实验**（下一步就做）：
`-Dlucistarlink.promptDispatch=true -Dlucistarlink.runtime.inlineBatchChanges=64 -Dlucistarlink.directSectionInstall=true`
→ 改动入队 → 立刻派发 → 在服务器线程上当场算完并直接写进引擎存储 → 延迟应降到与 SL 同量级（0.3~0.5 ms）。
代价：这条通道会**阻塞服务器线程**（与 SL 相同），所以它应当是一个**可选模式**（`runtimeLatencyMode` 或类似开关），默认保持异步（对 TPS 友好）；对外表述为"延迟优先模式可与 SL 持平，默认模式在重负载上更快"。
正确性门槛（与 SL 同类做法必须过的）：差分套件 + 相邻成对探针（已有 8 次 0 偏差的基线）+ 客户端边界用例 + `forceLightIncorrectOnSave` 的兜底论证。

### 10.9 `directSectionInstall` 的 n=25 定论：**无效**（第 13 个被否定的假设）

| 组 | n | 中位 | 比值 | p |
|---|---|---|---|---|
| OFF | 25 | 0.797 | — | — |
| ON | 25 | 0.806 | 0.989 | **0.568**（大样本近似） |

n=10 时看到的 −25% 是噪声。至此**交付侧全部 13 个假设被否定**：合并窗口（3 次）、通知扇出、发布分道、内联阈值、直装（3 次）、piggyback、promptDispatch、保留世界、稳定窗、光环/计算量、世界生成污染（这一条是*成立*的量具缺陷，已修）。

工具说明：`ab-compare.ps1` 的精确枚举只适用于 n≤20；n>20 自动改用正态近似（含连续性修正、逻辑斯蒂形式，因 Windows PowerShell 5.1 无 `[Math]::Erf`）。n=25 那次曾静默返回 NaN（枚举循环在大样本下空转），已修。

### 10.10 同步通道三件套（§10.8 的方案）：**无效果**（第 14 个被否定的假设）

`promptDispatch=true + inlineBatchChanges=64 + directSectionInstall=true` vs 全默认，洁净协议，n=10+10：

| 组 | 10 轮原始值（ms） | 中位 | p |
|---|---|---|---|
| 默认 | 1.378 1.578 **7.693** 0.887 1.499 0.833 **3.366** **5.212** 1.343 1.072 | 1.438 | — |
| 三件套 | 1.127 **5.210** 1.398 1.038 **14.326** 1.637 1.608 1.794 1.094 0.787 | 1.503 | **0.91** |

机制确认都生效（`promptDispatch.run`=9、`inlineExecuted`=54、直装 15 个 section）——**收益仍为零**。

**同时观察到一个重要的方法学事实**：本组**连基线都比当天早些时候慢一倍**（0.8 → 1.44，并出现 5~14 ms 离群）。这台机器连续跑了约 10 小时基准（多轮 MC 服务端 + Gradle），机器状态已漂移 → 又一次印证"**只有同场交错可比**"，以及"跨会话的绝对值不可引用"。

### 10.11 结论（14 个假设之后的判断）

`sky_hole` 上与 SL 的 ~2 倍差距**没有通过任何开关消掉**。证据链：
- 交付侧 13 个假设全部否定（含 n=25 的直装）；
- 计算量不在关键路径（光环 2.5~4.5× 的计算差异对 pass 零影响）；
- 诚实量具下 pass = apply + **一条固有的异步链路**（申请 → 线程跳 → 引擎吸收 → future 完成）≈ 0.75~0.85 ms，SL 的 0.31~0.43 ms 是它同步完成的全部成本。

**要持平只有一条路：让存储归我们所有（V2 的 storage 模式），使这条链路整体消失** —— 收益被实测限定在 ~0.4 ms（约 2× → 持平），代价是重写项目里最脆弱的发布/正确性层（halo、外部刷新、漂移重跑、存档安全、客户端同步全在这里）。
**建议**：要么按 V2 路线做（大工程 + 完整正确性验证），要么**接受这一档的 2×**，并把对外表述固定为"两个重负载快 1.8~2×、大面积改动持平、天空小改动慢 2×，正确性与稳定性全面占优"。两条路都需要用户拍板 —— 这是本轮唯一需要决策的地方。

### 10.12 冲突数据集已调和（2026-09-19，同会话 5 轮交错）

之前有两组互相矛盾的 `sky_hole` 数字（实施者 0.651 vs 0.593；监督者独立运行 0.924 vs 0.530，后者当时还有杂散守护进程），
协议里明确要求"同会话交错 ≥5 轮"来定论。已跑：

`AB_GROUP=g1 ab-interleaved.sh 5 dense_chunk_patch block_toggle_border structure_cube sky_hole`，
分组比较（`ab-compare.ps1 -GroupA g1-ours -GroupB g1-sl`，指标 = 每轮最小值，5 轮取中位，精确双侧 p）：

| 档位 | 我方中位 (ms) | SL 中位 (ms) | 比值 我方/SL | p | 读数 |
| --- | --- | --- | --- | --- | --- |
| `block_toggle_border` | **1.308** | 2.323 | 0.563 | 0.0079 | 我们快 1.78×，显著 ✓ |
| `structure_cube` | **2.753** | 3.749 | 0.734 | 0.6905 | 我们快 1.36×，**不显著**（我方 5 轮里 2 轮被环境突发污染成 9.9 / 7.3 ms） |
| `dense_chunk_patch` | 2.595 | **2.069** | 1.254 | 0.0317 | 我们慢 1.25×，显著 ✗ |
| `sky_hole` | 0.912 | **0.576** | 1.583 | 0.0079 | 我们慢 1.58×，显著 ✗ |

**`sky_hole` 的定论**：落后 **1.58×**（0.912 vs 0.576），与监督者那组（0.924 vs 0.530）一致，
实施者那组乐观的 0.651 / 0.593 **不成立于交错测量** —— 两组冲突记法作废，以本表为准。

**`dense_chunk_patch` 的定论**：本会话同场交错里我们**落后 1.25×**（这是修复"改方块守卫"之后的数字；修复前同会话是 1.83×）。
历史表里"dense 我们赢"（1.872 vs 2.31~2.87）**没有在本会话复现** —— "跨会话绝对值不可引用"这条规矩再次生效。
所以对外表述应该是：**两个负载赢（border 显著、structure 方向一致但 p 不显著）、两个输（dense 1.25×、sky 1.58×）**，
不是"三项领先"。

### 10.13 用户要求「干他、反超前辈模组」——本轮把代价与已测证据摆明面，并跑最后一条未测组合

用户 2026-09-19 的决定：不接受这一档的落后，要求反超。先把**代价**与**已经测过什么**列清，避免重复劳动：

**代价（同步路线的本质）**：ScalableLux 的 0.355~0.58 ms 是它在**服务器线程的 tick 内**算完并提交的全部成本；
要走这条路，就得把光计算与提交搬回 tick 里 —— 与我们「不阻塞服务器线程」的核心设计相反，**TPS 抖动风险**真实，
而且**会拿 border（1.78× 领先）与 structure（1.36× 领先）去换**。那两项是我们目前明确赢的两档。

**已经测过的「同步/即时」变体（全部是 ≥5 轮同场交错，别再重复）**：

| 变体 | 结果 |
| --- | --- |
| `promptRuntimePublish`（立即投递，不等合并窗口） | **1.114 vs 0.776，+44% 更差** |
| `piggybackPublish`（挂在原版任务上同轮提交） | **更差**，per-pass 0.48 → 1.24 ms 并出现一次 14.6 ms 停顿 |
| `promptDispatch`（入队即派发） | **显著更差**，0.715 → 0.820，n=10，p=0.043 |
| `directSectionInstall`（跳过引擎再吸收） | n=25：0.797 vs 0.806，比值 0.989，**p=0.568 中性** |
| `syncRuntimeDrain`（在 tick 内等自己的提交落地） | **中性** 0.784 |
| `inlineBatchChanges=64 + directSectionInstall + promptDispatch` 组合 | 机制确认生效（9 次派发、54 个内联作业、15 个直装 section），**1.438 vs 1.503，p=0.91 中性** |
| **V2 全局存储**（新架构，让存储归我们所有） | **0.815 vs 0.819，p=0.69 中性** —— 前提被自己的实现否证 |

**结论**：这条链上**没有剩余的可拉开关** —— 15+ 个交付侧杠杆与「新架构」本身都已实测。要反超，缺的不是再找一个开关，
而是把这条**异步链的固有成本**压到低于 SL 的同步成本，目前没有任何机制做到这一点。

**本轮仍在跑的最后一条未测组合**（用户要求"干他"，故补上）：`directSectionInstall + syncRuntimeDrain` 同时打开
（= 跳过引擎再吸收 **且** 在 tick 内等提交落地，最接近"同步算完再提交"的现有形态），
`EXTRA_JVM` 同场交错 5 轮对 ScalableLux。判据：这条链里我方 `sky_hole` per-pass 最小值的**中位数比 ≤ 1.00 且显著**，
才算反超；否则这条线就此定案接受。

### 10.15 逐字段拆解（2026-09-20）：差距不是计算、不是提交，而是「作业迟迟不跑」

把 `sky_hole` 每轮的所有字段摊开看（同一条交错链 `cr1`，我方 vs ScalableLux）：

| 字段 | 我方 pass4 / pass6 | ScalableLux pass4 / pass6 |
| --- | --- | --- |
| `elapsed` | 912 / 1898 µs | **503 / 588 µs** |
| `apply`（施加 25 个改动） | 119 / 107 µs | 87 / 80 µs ← **一样快** |
| **`wait`** | **528 / 1503 µs** | **3 / 314 µs** |
| **`sections`（发布了几个 section）** | **0 / 0** | 0 / 0 |
| **`identical`（算出来和引擎里一样）** | **11 / 5** | 0 / 0 |
| `drain` / `runUpd` / `publish` | 15 / 2 / 0 µs | 0 / 0 / 0 |
| **`queueLat`（入队 → 作业开始）** | **691 / 395 µs** | 0 |

**三个结论**（都是这次才看清的）：

1. **我们每轮花 0.5~1.5 ms，是在等自己的管线，而不是在算光照** ✗ —— `drain`/`runUpd`/`publish` 全是零头，
   而 `wait` 与 `queueLat` 几乎相等。
2. **这一档我们做的是「零产出的全流程」** ✗✓：`sections=0`（一个 section 都没发布）+ `identical=11`（算出来
   和引擎里逐字节相同）—— 因为该档是 air↔stone 来回切换，光每次又回到原值。**我们付了整条链的价钱，换来
   一句「不用改」**。
3. 因此 **「计算量不在关键路径」那条老结论要重新表述** ✓：砍计算不动 pass 时间，是因为 **pass 时间根本不是
   计算**，而是**派发延迟** ✗✓。之前所有基于「让计算更快」的杠杆注定无效（实测也确实是）✓。

**下一个实验（已经具体到代码）**：把「改动入队 → 运行时作业真正开始跑」这一段的**四个跳点各打一个时间戳**
（入队 / flush 内联 tick / 作业体开始 / 发布入队），跑一轮 `sky_hole`，看 395~691 µs 具体落在哪一跳。
候选（按怀疑度）：
- `runtimeManager.tick()` 把批次**交给工作池**而不是内联执行（`inlineRuntime` 声称内联，实测 50/54 ✓ 需复核）；
- 发布批次的 `delayedExecutor(250 µs)` 与 mailbox 往返（`syncRuntimeDrain=true` 已在 1.2.2 起默认开 ✓，
  理论上应绕过它，但 `wait` 没有下降 ✗ → 值得直接验证它是否真的生效）；
- `tickRuntime` 的**每次 tick 单批**限流（`maxBatchChunks` / 单批上限）导致大改动要等下一 tick。

**判据**：把 `sky_hole` 的 `wait` 从 500~1500 µs 压到 ≤150 µs（SL 的水平是 3~314 µs），且四项零回归。

### 10.16 结局：交付侧的收益已拿满（1.583× → 1.233×），再往前是结构问题（2026-09-20）

**拿到的**：
1. **完成信号不再等合并窗口**（`flushMarker` 立即触发 drain ✓）→ 我方 `wait` **528~1503 µs → 235~265 µs** ✓✓
   （与 SL 同级 ✓）→ `sky_hole` 5 轮交错中位数 **1.583× → 1.233×** ✓✓（历史最好，p=0.0079）；
2. flush 路径改为**无条件 drain** ✓（`published=false` 时旧代码整段跳过同步 drain ✗ —— 正是 sky_hole 这种
   「算完发现不用改」的负载）→ 实测中性 ✓，保留（语义更正确 ✓）。

**试过并否掉的**（都带数字）：
- **服务器线程直提**（`-Dlucistarlink.inlineDrain=true` ✓）：`min` **2482~7294 µs**、`wait` **1723~6553 µs** ✗✗
  —— **约 10 倍回退** ✗。原因：在服务器线程装数据**阻塞了服务器线程** ✗，而**没有**省掉引擎自己那一趟
  `runLightUpdates`（它照样得跑 ✗）→ 净增加 tick 内的工作 ✗✓。**开关保留、默认关、附此数字，防止重复尝试。**

**结论（这就是这一档的答案）**：那条跨线程交接**不是调度选择，而是我们引擎结构本身** ✗ ——
除非把引擎设计换成他们的（V2 全局存储已经试过：**0.815 vs 0.819，p=0.69 中性** ✗），否则
**交付侧的天花板就是 1.233×** ✓。此事到此可以用测量结论收尾，而不是靠意见 ✓。

**又补测并否掉的两条（2026-09-20，都带数字）**：
- **不标记**（只在引擎还不认识该 section 时才 `updateSectionStatus`，其余直接装）：中位数
  **1.129 vs 0.595 = 1.898×** ✗（比 1.233× 差，还有一轮 7337 µs 离群）→ 说明那一步登记**是必需的** ✗，
  已回退 ✓；
- 加上前述「服务器线程直提」的 10 倍回退 ✗ —— **两条路都被测量封死** ✓✓。

**因此「持平 sky_hole」在当前架构下不可达** ✗ —— 这不是没试，而是**每个机制都有数字** ✓✓：
我们交付的 17+ 个杠杆（含本轮两条最有希望的）全部中性或更差，而唯一剩下的路（换引擎设计）已被 V2 实测否掉 ✓。

### 10.14 结果：**明显收窄，但没有反超** —— 1.256×，p=0.0556（2026-09-19）

同场交错 5 轮（`AB_GROUP=sync2`，`EXTRA_JVM="-Dlucistarlink.directSectionInstall=true -Dlucistarlink.syncRuntimeDrain=true"`，
`ab-compare.ps1` 输出）：

| 组 | 每轮最小值（ms） |
| --- | --- |
| 我方（同步组合） | 0.570 / 0.705 / 0.719 / 1.115 / 0.732 → **中位 0.719** |
| ScalableLux | 0.572 / 0.586 / 0.423 / 0.569 / 0.561 → **中位 0.573** |

**比值 1.256，U=22/25，精确双侧 p=0.0556。**

**怎么读这个数**：
- 默认配置在同一会话的对照组（`g1`）里是 **0.912 vs 0.576 = 1.583×，p=0.0079（显著落后）**；
  而 SL 在两组的读数几乎相同（0.576 / 0.573）说明基线稳定 —— 所以**这两个开关确实把我们自己的天空档拉近了约 21%**
  （0.912 → 0.719），差距从「显著落后」变成「边缘、不显著」。
- 但**没有反超**：中位数仍在 SL 之上，p 也没过 0.05。要宣称反超，必须中位比 ≤ 1.00 且显著 —— 现在不是。
- 诚实结论：**这条线的固有成本还剩约 0.15 ms**（0.719 - 0.573），而所有能拉动的机制都已试过（§10.13 表）；
  剩下的就是「异步交付链 vs 同步完成」本身。**对外表述建议：两个负载明显更快、天空小改动慢 1.26×（不再显著）。**

**顺带：探针闸抓到一个真 bug 并已修复（1.2.1）**。开启这两个开关后跑相邻对探针时，出现
`LayerLightEventListener$DummyLightLayerEventListener cannot be cast to LightEngineAccessor` ——
1.1.5 给发布路径加的「同时写入可见层」在**没有光照引擎的层**（无方块光的维度）上会抛 `ClassCastException`（Worker 线程，
被 `Util` 捕获后只留一行日志）。已加类型判断（没有引擎就直接返回，本来就没什么可装的），
修复后探针 **3/3 轮与引擎关闭对照逐位相同**（`edit` `013c3846466ff9d5` / `edit_border` `c4aed9ab13e5d3b5`），
`ClassCastException` 归零。**这也说明：翻默认之前跑探针闸不是形式主义。**

## 12. 第三方复核（2026-09-21）：三个开关无收益；协议本身有一个偶发卡死

按 §10.3 与 §10.7 的口径，在**另一台机器**上复核了三项，结论都是**否定**。

### 12.1 先交代环境，否则数字会骗人

本机噪声明显大于原作者那台：**同一构建、同一配置、相邻两轮** `minPassNanos` 差 1.5 倍
（1,654,000 vs 2,528,400 ns）。所以下面只看**变体之间的相对顺序**，不看绝对值。

这也意味着：本机能可靠分辨的是「几十个百分点量级」的差别，`sky_hole` 这条线剩下的
那点余量（§10.16 的 1.233×）在本机**测不出来**。

### 12.2 `syncSmallEdits`：机制生效，但无收益

先证明开关真的走到了 —— `lucistarlink.syncSmallEdits.routed` 在 light 阶段计数 **18**，
说明 `drained>0 && drained<=512 && regionChunks==1` 这条同步路径确实被命中
（`sky_hole` 每 pass 25 次 `setBlock` ≈ 150 条记录，正落在窗口内）。

`sky_hole`，2 轮交错，每趟 `passes=3 warmup=1`，`minPassNanos`（ns）：

| 变体 | r1 | r2 |
| --- | --- | --- |
| 默认 | 1,654,000 | — |
| `syncSmallEdits=true` | 2,181,000 | — |
| `promptDispatch=true` | 2,576,000 | 1,556,000 |
| `piggybackPublish=true` | 1,725,000 | 1,453,000 |

没有任何一组落在默认组的噪声带之外，`syncSmallEdits` 那组反而更高。
方向与 §10.16 已否掉的 `inlineDrain`（服务器线程直提，约 10 倍回退）一致：
**把交付搬到调用线程不划算**，因为引擎自己那一趟 `runLightUpdates` 照样得跑，净增 tick 内的工作。

### 12.3 协议缺陷：prepare 会偶发卡住（建议修）

`prepareRing=3` 时 prepare 偶发停在 `loaded=127/144 pending=17` 不动；
`prepareRing=1` 出现**同形状**的 `loaded=49/64 pending=15`（这一次后来自己过了）。

**所以与 ring 无关**，是 prepare 阶段偶发有区块加载不出来。`LuxServerBenchmark` 目前只是
反复打印同一行 `prepare waiting`，没有超时也没有卡住区块的坐标 —— 一次测量会静默停在那里，
要等多久完全取决于运气。建议加可配置超时，并把卡住的区块坐标打出来。

### 12.4 一条方法论（本轮为此误判了三次）

`... | Select-String | Select-Object -Last N` 会把输出**缓冲到命令结束**才吐。
一次 26 秒就能跑完的测量，用这种过滤看起来完全像死机。跑基准请 `Tee-Object` 落盘再读文件。

### 12.5 与 §10.16 的关系

§10.16 的结论是「交付侧收益已拿满，持平在当前架构下不可达」。本轮是**独立的第二台机器复核**，
没有推翻它，只是又否掉了三个开关（`syncSmallEdits` / `promptDispatch` / `piggybackPublish`），
并补上一个与性能无关、但会浪费别人时间的协议缺陷。

## 13. 三方对比（2026-09-21，默认协议）

`minPassNanos` 中位数、3 轮交错、**默认协议**（`prepareRing=1` / `quiesceSettleMs=0`）。
三方 = `master`（v1.2.8）/ 已合并的跨区清除修复 / ScalableLux。

| 负载 | 权重 | 修复后 | `master` | SL | 修复后÷SL | `master`÷SL |
| --- | --- | --- | --- | --- | --- | --- |
| `block_toggle_border` | 45% | 1.669 | 2.339 | 1.345 | 1.241 | 1.739 |
| `structure_cube` | 35% | 3.928 | 4.767 | 4.849 | 0.810 | 0.983 |
| `dense_chunk_patch` | 15% | 4.560 | 5.115 | 4.537 | 1.005 | 1.127 |
| `sky_hole` | 5% | 1.715 | 2.029 | 1.517 | 1.131 | 1.338 |
| **加权（Σw·t）** | | **2.896** | **3.590** | **3.059** | **0.947** | **1.174** |

以 SL = 1.000 计：修复后 **0.947**（快 5.6%），`master` **1.174**（慢 17.4%）。

**两个必须一起读的限定，否则这张表会被误用**：

1. **「修复后 vs `master`」那 24% 不建议当作这条修复的性能收益。** 两批独立测量方向一致（快 22% 与 40%），
   但这条修复的门控只在「同一 tick 出现 ≥2 个改动数 ≥64 的区块」时触发，`block_toggle_border` 那种单点编辑
   根本不会走到。这个差更可能来自两侧运行环境不同（主工作树与参照 worktree 的构建/预热状态不一致），而不是代码。
2. **`÷ SL` 那一列需要更多轮次才能定。** SL 在第二批（同协议、同档）把 `block_toggle_border` 测成
   **2.554 ms**，是本表 1.345 ms 的 **1.9 倍**。批次间漂移有这么大，这一列就不该只靠 3 轮。

**另外注意**：本表是**默认协议**，与 §7.9 / §10.14 的洁净协议数字**不可直接比较**（§7.8 已经说明旧协议会
把两边的数字都推高、而且把 SL 推得更多）。

## 14. 本机复核 §13 的三方表（2026-09-21，洁净协议，jar 对 jar）

§13 那张表自己标了两个限定：3 轮不够、以及「修复后 vs `master`」那 24% 更可能来自两侧运行环境不同。
这里用**同环境、只换 jar** 的交错 A/B 把第二个限定直接判掉：`jar-ab.sh`（洁净协议
`prepareRing=3` / `quiesceSettleMs=1000`，`passes=4 warmup=2`），5+5 轮交错，指标仍为每轮
`minPassNanos` 最小值的中位数，精确双侧 Mann-Whitney（n=5+5，U/25）。

| 负载 | 门槛是否触发 | 1.2.8 中位 (ms) | 1.2.9 中位 (ms) | 比值 | U/25 | p |
| --- | --- | --- | --- | --- | --- | --- |
| `block_toggle_border` | 否（单点编辑） | 0.772 | 0.726 | 1.063 | 16/25 | 0.55 |
| `dense_chunk_patch` | 是（跨区块批量） | 2.049 | 2.208 | 0.928 | 11/25 | 0.84 |

**结论一（判掉 §13 的第二个限定）**：那条修复**没有 24% 的性能收益**。对照组（门槛不触发的单点编辑档）
两侧**没有差别**（p=0.55），靶档（跨区块批量）方向相反、**慢约 7.8% 但远未达显著**（U=11/25，p=0.84）。
也就是说 §13 里那 24% 是**环境/预热差异**，与作者自己的判断一致。

**结论二（这条修复的代价）**：唯一可能变慢的是「同一 tick 出现 ≥2 个改动数 ≥64 的区块组」那种批量清除，
方向上确实略慢（少了一些并行），量级 ≤8% 且在当前轮次下不显著。用一个**会永久写进存档的残留光照**
换这点并行度，按本仓库「正确性优先、且任何数字都要能被同会话交错复核」的口径，这个交换是值得的。

**还没做的**：`structure_cube` 与 `sky_hole` 两档本轮没跑（跑完两档后被中止），以及 §13 里的 `÷ SL`
那一列——那需要**每个构建各跑一组 vs ScalableLux**（约 1 小时负载），本机噪声带又宽，建议按 ≥5 轮、
洁净协议来跑。

## 15. 本机用第三方的 `threeway.ps1` 跑完整三方（2026-09-21，默认协议，6 轮拉丁方）

工具、协议、指标都来自第三方脚本本体（`threeway.ps1`，**未做任何改动**）：仓库自己的 gradle 任务
（`runBenchmarkServer` × 2 + `runBenchmarkScalableLuxServer`），`passes=3 warmup=1`，**默认协议**
（`prepareRing=1` / `quiesceSettleMs=0`，与 §13 同口径，与 §7.9/§10.14 的洁净协议**不可直接比**），
4 档 × 3 侧 × 6 轮 = 72 趟，18 分钟。p 值按作者 README 的指示：csv → jsonl 后喂本仓库的
`mc-smoketest/ab-compare.ps1`（n=6+6，精确双侧）。

| 负载 | 权重 | 我们 1.2.9 | 上游 1.2.8 | SL | 我们÷SL | 上游÷SL |
| --- | --- | --- | --- | --- | --- | --- |
| `block_toggle_border` | 45% | 1,293,100 | 1,252,850 | 1,128,750 | 1.146 | 1.110 |
| `structure_cube` | 35% | 7,801,750 | 7,725,100 | 8,691,350 | **0.898** | 0.889 |
| `dense_chunk_patch` | 15% | 5,550,650 | 5,392,150 | 4,702,900 | 1.180 | 1.147 |
| `sky_hole` | 5% | 1,305,100 | 1,161,250 | 841,700 | **1.551** | 1.380 |
| **加权 Σw·median** | | **4,210,360** | **4,134,453** | **4,297,430** | **0.980** | **0.962** |

**逐档 p 值（精确双侧，U/36）**

| 比较 | `block_toggle_border` | `structure_cube` | `dense_chunk_patch` | `sky_hole` |
| --- | --- | --- | --- | --- |
| 我们 vs 上游 | 1.032 · p=0.94 | 1.010 · p=1.00 | 1.029 · p=0.39 | 1.124 · p=0.31 |
| 我们 vs SL | 1.146 · p=0.31 | **0.898 · p=0.0022（U=0/36）** | 1.180 · p=0.093 | **1.551 · p=0.0022（U=36/36）** |
| 上游 vs SL | 1.110 · p=0.48 | 0.889 · p=0.065 | 1.147 · p=0.093 | 1.380 · p=0.18 |

**三条结论**

1. **PR #2 这条修复性能上是中性的。** 与上游逐档比，没有任何一档达到显著（最小 p=0.31，比值都在 ±12% 内），
   与 §14 的 jar 对 jar 结果方向一致。用「消除一条会写进存档的残留光照」换这点噪声，值得。
2. **对 SL：一档决定性领先、一档决定性落后、两档未分。** `structure_cube` 完美分离（U=0/36，p=0.0022，
   快 10%）；`sky_hole` 也完美分离但方向相反（U=36/36，p=0.0022，慢 55%）；`dense_chunk_patch` 倾向落后
   （1.180，p=0.093，未达显著）；`block_toggle_border` 未分（p=0.31）。
3. **加权 0.980**（我们对 SL），与第三方在 §13 的 0.947 **同向**（都在 SL 附近、互有胜负），
   差别来自轮数与机器，不是代码 —— 本表 6 轮、§13 3 轮。**注意这张表是默认协议，
   不能与 §7.9/§10.14 的洁净协议数字混用**（§7.8 已证明旧协议把两边都推高、且把 SL 推得更多）。

### 15.1 两批三方放在一起：那 24% 是环境，不是代码

| | `block_toggle_border` | `structure_cube` | `dense_chunk_patch` | `sky_hole` | 加权 |
| --- | --- | --- | --- | --- | --- |
| §13（第三方机器）我们÷SL | 1.241 | 0.810 | 1.005 | 1.131 | 0.947 |
| §15（本机）我们÷SL | 1.146 | 0.898 | 1.180 | 1.551 | 0.980 |

**方向逐档一致**（`structure_cube` 都赢、`sky_hole` 都输、另两档都略输），**绝对值差很多**
（他 `structure_cube` 3.93 ms、本机 7.80 ms，跨机器差 2 倍 → 绝对值不可跨机器比）。

**他那版的「上游」列被环境系统性压慢**：上游÷我们在他那台是 1.401 / 1.214 / 1.122 / 1.183（全线慢 12~40%）；
在本机是 0.969 / 0.990 / 0.971 / 0.890（互有胜负，且四档**没有一档显著**，最小 p=0.31）。
所以 §13 那个「我们比上游快 24%」是**上游那侧的环境差**，不是这条修复的收益 —— 与他自己的判断一致，
也与 §14 的 jar 对 jar 结果一致（同环境只换 jar：对照档 p=0.55、靶档 p=0.84，均无显著差别）。

## 16. 脏环境下的三方（2026-09-21，同一脚本、同一协议、同 6 轮）

用户要求「在性能影响下跑一次对比」——正好是他们真实机器的常态（后台有占用 CPU 的任务）。工具与协议与 §15
完全一致（`threeway.ps1`，`passes=3 warmup=1`，4 档 × 3 侧 × 6 轮 = 72 趟，21.5 分钟），所以这张表可以**逐格
和 §15 的干净表比**。

| 负载 | 权重 | 我们 | 上游 | SL | 我们÷SL | 上游÷SL |
| --- | --- | --- | --- | --- | --- | --- |
| `block_toggle_border` | 45% | 4,394,400 | 6,729,150 | 2,442,650 | 1.799 | 2.755 |
| `structure_cube` | 35% | 15,321,100 | 15,507,600 | 8,953,900 | 1.711 | 1.732 |
| `dense_chunk_patch` | 15% | 7,461,200 | 14,695,350 | 5,551,350 | 1.344 | 2.647 |
| `sky_hole` | 5% | 5,209,200 | 7,508,550 | 1,135,900 | **4.586** | 6.611 |
| **加权** | | **8,719,505** | **11,035,508** | **5,122,555** | **1.702** | **2.154** |

**脏 vs 干净（同一指标、同一档，逐格比）**

| 负载 | 我们变慢 | 上游变慢 | **SL 变慢** |
| --- | --- | --- | --- |
| `block_toggle_border` | 3.40× | 5.37× | 2.16× |
| `structure_cube` | 1.96× | 2.01× | **1.03×** |
| `dense_chunk_patch` | 1.34× | 2.73× | 1.18× |
| `sky_hole` | 3.99× | 6.47× | 1.35× |
| **加权** | **2.07×** | **2.67×** | **1.19×** |

**逐档显著性（n=6+6，精确双侧）**：我们 vs SL —— `dense` 1.344（U=36/36, p=0.0022）、`sky_hole` 4.586
（U=36/36, p=0.0022）、`structure_cube` 1.711（U=33/36, p=0.0152）**三档决定性落后**，`border` 1.799 未达显著
（p=0.24）。我们 vs 上游 —— `dense` **0.508（我们快 2×）**、`sky_hole` 0.694、`border` 0.653、`structure_cube`
0.988，即**脏环境下我们全面优于上一个发布版**。

**三条结论**

1. **异步架构对机器负载敏感，同步架构不敏感。** 脏环境下我们加权慢 **2.07×**、SL 只慢 **1.19×**；
   `structure_cube` 上 SL 甚至只慢 3%（1.03×）而我们慢 1.96× —— 于是干净窗口里我们 0.898 领先的档，
   在脏环境下变成 1.711 落后（p=0.0152）。原因是机制性的：我们的每趟终点是引擎在 tick 上的调度，
   机器一慢它跟着慢；SL 在 `setBlock` 里同步做完，不吃 tick 调度。
2. **但我们的异步管线本身比上一个发布版更抗压**：同样异步的前提下，脏环境里 `dense` 我们快 2×、
   `sky_hole` 快 1.44×、`border` 快 1.53× —— 交付侧那些优化**恰好在这种机器上兑现**。
3. **对 own-engine 立项的意义**：用户机器就是脏的那台，所以「只落后 0.4 ms/次、排行榜级」的说法只在干净窗口
   成立；脏环境下 `sky_hole` 差 4.6×、加权差 1.7×，是**玩家能感觉到的量级**。而唯一能同时拿掉「tick 调度依赖」
   和「交付链」的正是同步的就地引擎 —— 这条测量的价值在于：它把 own-engine 的收益从"干净窗口追平"
   提升到"脏环境下的架构性优势"。

## 17. `syncSmallEdits` 的第二次否证（2026-09-21，n=12+12）

一轮 5 轮的 A/B 曾给出「打开 `syncSmallEdits` 快 **25.6%**」（1.095 → 0.872 ms，U=17/25，**p=0.42**）。
按仓库规矩「比值要在独立复现组里重现才算结果」，用 12 轮重跑：

| 组 | 关（false） | 开（true） | 比值 | U | p |
| --- | --- | --- | --- | --- | --- |
| **`sky_hole`（12+12）** | **0.786** | **1.107** | **0.709** | 39/144 | **0.079** |
| `block_toggle_border`（6+5） | 1.189 | 0.984 | 1.207 | 19/30 | 0.537 |

**结论：打开后 `sky_hole` 慢 41%（p=0.079）**，与当年「健康窗口慢 12~20%」那条否证同向；border 档方向相反
但不显著。**默认保持关闭。**

**方法论教训（值得记）**：n=5 的这一档噪声足以造出一个 +25.6% 的假信号 —— 本项目 5 轮的判据在 `sky_hole` 上
**不够**，要么 ≥10 轮、要么必须有第二组复现。这一条与 §7.8 的"同会话交错"并列，作为 `sky_hole` 测量的最低要求。
